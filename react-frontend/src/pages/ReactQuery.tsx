import React from 'react';
import {GameListTanstack} from "../components/games/GameListTanstack";

const ReactQuery: React.FC = () => {
    return (
        <section>
            <GameListTanstack />
        </section>
    );
};

export default ReactQuery;
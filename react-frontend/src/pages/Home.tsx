import React from 'react';
import { GameList } from '../components/games/GameList';

const Home: React.FC = () => {
    return (
        <section>
            <GameList />
        </section>
    );
};

export default Home;

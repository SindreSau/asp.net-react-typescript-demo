import React from 'react';
import {GameListAxios} from "../components/games/GameListAxios";

const Axios: React.FC = () => {
    return (
        <section>
            <GameListAxios />
        </section>
    );
};

export default Axios;
